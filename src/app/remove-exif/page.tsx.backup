import type { Metadata } from "next";
import { RemoveExifClient } from "./RemoveExifClient";

export const metadata: Metadata = {
	title: "Remove EXIF Data Online — Free & Private",
	description: "Strip EXIF metadata, GPS location, camera info from your photos. 100% private — files never leave your browser.",
	keywords: ["remove exif data", "strip metadata", "remove gps from photo", "delete exif online", "remove image metadata"],
};

export default function RemoveExifPage() {
	return <RemoveExifClient />;
}
