import type { Metadata } from "next";
import { ResizeImageClient } from "./ResizeImageClient";

export const metadata: Metadata = {
	title: "Resize Image Online — Free & Private",
	description:
		"Resize images online for free by percentage or exact dimensions. Social media presets included. 100% private — files never leave your browser.",
	keywords: ["resize image", "resize image online", "image resizer", "resize photo online free"],
};

export default function ResizeImagePage() {
	return <ResizeImageClient />;
}
